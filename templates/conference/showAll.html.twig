{% extends 'base.html.twig' %}

{% block title %} Conference {% endblock %}

{% block body %}

    <div class="example-wrapper">

        <a href="{{ path("searchConference") }}"> <button> Rechercher une Conference </button></a>
        <h3> Conference's List : </h3>
        <ul class="listConference">
        {% for conference in conferences %} 
            <li> {{ conference.name }}
            {% if NbEtoile is defined %}
                {% for i in 1 .. NbEtoile %}

                    <a href="{{ path("oneVote",{'id':conference.id,'nbre':i }) }}" class="etoile-{{ conference.id }}-{{ i }}"> <i class="material-icons star">star_border</i> </a>
                {% endfor %}
            {% endif %}
            </li>
        {% endfor %}
        </ul>
        <div class="navigation">
            {{ knp_pagination_render(conferences) }}
        </div>
    </div>
{% endblock %}

{% block script_javascripts  %}
    <script>
        //TODO: A developper plus tard
    $(".star").hover(function(){
        var classLigne = this.parentNode.className;
        var ligneSplit = classLigne.split('-');

        for(var i = 1; i <= ligneSplit[2]; i++){
            //var nomclass = ligneSplit[0] + "-" + ligneSplit[1]+ "-" + i ;
            //$(nomclass).innerHTML = "<i class=\'material-icons star\'>star</i>";
        }
    })

    </script>
{% endblock %}
